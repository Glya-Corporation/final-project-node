const { ProductInOrderServices } = require("../services");

const addProductToOrder = async (req, res, next) => {
    try {
        const {orderId, productId, quantity} = req.body
        
    } catch (error) {
        
    }
}